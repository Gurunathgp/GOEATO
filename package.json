{
  "name": "goeato-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "GoEato - Modern Full-Stack Food Delivery Platform",
  "scripts": {
    "dev": "concurrently -n \"backend,frontend\" -c \"cyan,magenta\" \"npm --prefix goeato-backend run dev\" \"npm --prefix GOEATO-frontend run dev\"",
    "build": "npm --prefix GOEATO-frontend run build",
    "test": "npm --prefix goeato-backend test",
    "seed": "npm --prefix goeato-backend run seed",
    "install:all": "npm install && npm --prefix goeato-backend install && npm --prefix GOEATO-frontend install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
